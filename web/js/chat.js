/**
 * Chat Page (placeholder)
 * Chat functionality removed — WebSocket is managed at app level (index.html).
 * This file is kept minimal for future chat system implementation.
 */

import WS from './websocket.js';

const Chat = {
    init() {
        // Chat disabled — WebSocket managed by Footer in index.html
    },

    unload() {
        // Chat disabled — no cleanup needed
    },
};

export default Chat;
